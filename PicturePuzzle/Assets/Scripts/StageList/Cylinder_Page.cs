using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// 지금은 안쓰는 예시용 스크립트. 
/// 실제 페이지 넘기는 기능은 StageListManager와 BookController가 담당한다.
/// </summary>

public class Cylinder_Page : MonoBehaviour
{
    /*  예시용 페이지 자동 넘기기 구현을 위한 부분.  
     *  예를 들때는 애니메이션 이벤트로 이 함수를 호출해줬었다. 
    public void FlipIsOver()
    {
        FindObjectOfType<BookController>().CallWhenFlipOver(gameObject);
    }
    */
}
